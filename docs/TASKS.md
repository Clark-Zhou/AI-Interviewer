# Tasks

## 文档职责

记录已经批准、正在等待执行或正在执行的任务卡，包括目标、范围、非目标、验收标准、状态和按需阅读文件。候选方向归 `docs/ROADMAP.md`。

## 当前任务卡

### 文档治理收尾（维护任务）

- 状态：已完成并修复首轮审查问题，待复审。
- 目标：修正项目路径，收敛活跃文档职责，明确多 session 维护边界和 archive 只写不读规则。
- 范围：`AGENTS.md`、根 README、活跃 docs、session 模板和旧入口兼容说明；不修改 PRD、archive 既有内容或功能代码。
- 验收：每类信息有唯一维护位置；所有 session 明确禁止读取 archive；根 README 与实际目录一致；旧兼容入口保留并有退出条件。

下一项开发工作必须先由产品助理 session 建立任务卡。`TASKS.md` 可以同时存在多个已批准任务，但一个阶段开发 session 默认只处理用户指定的一张任务卡，不得自行从 ROADMAP 候选方向推断任务。

开发 session 可以更新执行状态和结果，但不能自行扩大范围或修改验收标准；范围变化必须交回产品助理 session。
