# Roadmap

## 文档职责

记录未来阶段、当前优先级、候选方向和明确暂缓事项。已上线事实归 `docs/STATUS.md`，具体任务范围和验收标准归 `docs/TASKS.md`。

关联文件：

- `docs/TASKS.md`：当前任务卡和验收标准。
- `docs/HANDOFF.md`：当前交接状态。
- `docs/STATUS.md`：当前项目事实摘要。
- `docs/PRD.md`：产品边界，按需阅读。

## 当前推荐方向

阶段 1-25 已完成、审查并合并到 `main`。阶段 26 需要由产品助理 session 明确规划后再进入开发。

## 阶段索引

- [x] 阶段 1-25：已完成、审查并合并到 `main`。
- [ ] 阶段 26：待产品助理 session 规划。

## 当前优先级

1. 由产品助理 session 明确阶段 26 的目标、范围、非目标和验收标准。
2. 在 `docs/TASKS.md` 建立新的当前任务卡，并同步必要的交接信息。
3. 阶段开发 session 按新任务卡闭环实现，不直接沿用阶段 24/25 的分支任务。

## 候选方向

- 搜索或恢复历史 session。
- 云端历史记录。

候选方向不等于已批准任务；只有写入 `docs/TASKS.md` 的任务卡才可进入开发。

## 暂缓事项

- 云端文件存储、OCR、图片识别或复杂文件解析。
- 语音或视频面试。
- 未规划的 AI 逐步提问、多轮追问或单题即时批改。
- 用户资料页、OAuth、支付或权限系统。
- 复杂视觉重构或全站设计系统重构。

## 更新规则

- ROADMAP 由产品助理 session 维护，只保留当前和未来，不复述已上线能力或历史阶段详情。
- ROADMAP 用来记录阶段级方向、阶段索引和优先级，适合一次讨论多个阶段后更新。
- 单个具体任务的目标、范围、验收标准和审查关注点优先写在 `docs/TASKS.md`。
- 阶段内开发、审查和修复一般只更新 `docs/TASKS.md`、`docs/HANDOFF.md` 和必要的 `docs/STATUS.md`，不需要每次都改 ROADMAP。
- 当新增/完成一组阶段、调整阶段顺序、改变产品方向或暂缓事项时，再更新 ROADMAP。
- 如需归档，只能根据当前已知内容直接写入新的 `docs/archive/` 文件；任何 session 都不得读取 archive。
