# Handoff

## 文档职责

记录当前交接状态。新 session 默认读本文件；本文件应覆盖更新，不要变成长篇聊天记录。

## 当前状态

- 当前分支：`codex/optimize-homepage`
- 当前阶段：阶段 24/25 已合并；阶段 26 已规划，待开发。
- 当前任务：任务卡 #26，优化主页字体、短文案和信息排版，预留帮助/公告入口。
- 下一步：阶段开发 session 按 `docs/TASKS.md` 的任务卡 #26 实现并完成必要验证。

## 最近完成

- PR #8 已合并到 `main`，阶段 24/25 已完成、审查并合并。
- 已从最新 `main` 创建 `codex/optimize-homepage` 分支。
- 产品助理 session 已完成任务卡 #26，固定主页短文案、范围、非目标、验收标准和开发/审查提示词。
- 阶段 26 限定为主页局部优化，不扩展为全站视觉重构。

## 当前未完成

- 任务卡 #26 尚未实现和审查。
- 帮助页和公告页本阶段只预留入口，不实现页面或功能。
- 旧文档兼容入口先使用一段时间，确认新结构稳定后再考虑删除或彻底归档。

## 给下个 session 的提醒

- 默认只读 `AGENTS.md`、`docs/HANDOFF.md`、`docs/TASKS.md`。
- 不要默认读取 `docs/PRD.md` 或 `docs/archive/`。
- 如需了解当前事实，读 `docs/STATUS.md`。
- 当前只执行任务卡 #26，不要顺手实现帮助页、公告页或全站字体调整。
- 本任务不改 API、prompt、认证协议或历史数据结构。
- 完成开发后更新必要的 `docs/STATUS.md`、`docs/TASKS.md` 和本文件，再交给代码审查 session。
